import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Parcial1_movil',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
